name: Platinum Governor Validation
on: [push, pull_request]
jobs:
  trust_validation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Validate Trust Score & Approval
        run: |
          python platinum/governor.py --check --agent=codex --score-threshold=0.75
